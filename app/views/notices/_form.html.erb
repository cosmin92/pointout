<%= form_with(model: notice, local: true) do |form| %>
  <% if notice.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(notice.errors.count, "error") %> prohibited this notice from being saved:</h2>

      <ul>
      <% notice.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :title %>
    <%= form.text_field :title, id: :notice_title %>
  </div>

  <div class="field">
    <%= form.label :content %>
    <%= form.text_area :content, id: :notice_content %>
  </div>

  <div class="field">
    <%= form.label :posting_date %>
    <%= form.date_select :posting_date, id: :notice_posting_date %>
  </div>

  <div class="field">
    <%= form.label :expiration_date %>
    <%= form.date_select :expiration_date, id: :notice_expiration_date %>
  </div>

  <div class="field">
    <%= form.label :notice_type %>
    <%= form.text_field :notice_type, id: :notice_notice_type %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
